# Codex Keep 0.1.31

Adds Secondary Machine Mode for keeping clean Git repositories fresh.

- Adds a local-only menu toggle for secondary developer machines.
- Pulls Git repositories in `~/Repositories` every 30 minutes when enabled.
- Skips dirty, detached, no-upstream, and conflicting repositories.
- Adds a manual pull action and repository pull log.
